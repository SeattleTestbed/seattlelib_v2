from distutils.core import setup
setup(
  name = 'seattlelib_v2',
  packages = ['seattlelib_v2'], 
  version = '0.1',
  description = "Seattle's seattlelib package",
  author = 'Seattle',
  url = 'https://github.com/choksi81/seattlelib_v2', # use the URL to the github repo
  classifiers = [],
)