#this unit test checks the functionality of the sin fuinction 


#pragma repy restrictions.default math.r2py

GRAPHCOS = [1,0,-1,0,1]

# Checks values form 0 to +- 2 pi
count = 0
while(count != 4):
	if(math_cos((math_pi*count) / 2) != GRAPHCOS[count]):
		log("graphing cos was a failure")
	if(math_cos((-math_pi*count) / 2) != -GRAPHCOS[count]):
		log("graphing cos was a failure")
  count += 1