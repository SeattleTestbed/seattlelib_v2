"""
Verify that dylink respects the supplied file extensions when importing 
a library, i.e. filename.py and filename.r2py are distinct files 
(yet they represent identically-named modules, so the usual overriding 
rules apply, see #....)
"""

